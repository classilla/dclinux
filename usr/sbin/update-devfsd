#!/bin/sh
echo "# automatically generated by update-devfsd; do not edit" > /etc/devfs/dir.conf
for i in `echo /etc/devfs/conf.d/* | sort`; do
	[ ! -e "$i" ] && break
	echo 'INCLUDE				'"$i"
done >> /etc/devfs/dir.conf
if [ "$1" != "-n" ]; then
	/etc/init.d/devfsd force-reload
fi
