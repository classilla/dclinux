#!/bin/sh -e
# Automatically added by dh_installdocs
if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/doc/apache-common ]; then
	rm -f /usr/doc/apache-common
fi
# End automatically added section
# Automatically added by dh_installdocs
if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/doc/apache-common ]; then
	rm -f /usr/doc/apache-common
fi
# End automatically added section
