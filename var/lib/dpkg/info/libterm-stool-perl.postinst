#!/bin/sh -e
# Automatically added by dh_installdocs
if [ "$1" = "configure" ]; then
	if [ -d /usr/doc -a ! -e /usr/doc/libterm-stool-perl -a -d /usr/share/doc/libterm-stool-perl ]; then
		ln -sf ../share/doc/libterm-stool-perl /usr/doc/libterm-stool-perl
	fi
fi
# End automatically added section
