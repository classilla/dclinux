#!/bin/sh -e
# Automatically added by dh_installdocs
if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/doc/ncurses-base ]; then
	rm -f /usr/doc/ncurses-base
fi
# End automatically added section
