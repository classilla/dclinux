#!/bin/sh -e
# Automatically added by dh_suidregister
if [ "$1" = remove -a -e /etc/suid.conf ] && \
   command -v suidunregister >/dev/null 2>&1; then
        suidunregister -s jfbterm /usr/bin/jfbterm
fi
# End automatically added section
