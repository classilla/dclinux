#!/bin/sh -e
# Automatically added by dh_installdocs
if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/doc/libterm-slang-perl ]; then
	rm -f /usr/doc/libterm-slang-perl
fi
# End automatically added section
