#!/bin/sh
set -e
/etc/init.d/cron stop 

# Automatically added by dh_installdocs
if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/doc/cron ]; then
	rm -f /usr/doc/cron
fi
# End automatically added section

