#!/bin/sh -e
# Automatically added by dh_installdocs
if [ "$1" = "configure" ]; then
	if [ -d /usr/doc -a ! -e /usr/doc/logrotate -a -d /usr/share/doc/logrotate ]; then
		ln -sf ../share/doc/logrotate /usr/doc/logrotate
	fi
fi
# End automatically added section
